const a = 10
const b = 20

const str = "a value is " + a + " ,b value is " + b + " ,sum of a & b is " + (a+ b)
console.log(str)

const templateStirng = `a value is ${a}, b value is ${b}, sum of a & b is ${a+b}`
console.log(templateStirng)